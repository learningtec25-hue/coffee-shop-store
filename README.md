# Brew & Bloom Coffee Shop

A polished full-stack coffee shop ordering experience.

## Run locally

### Backend
```bash
cd backend
npm install
npm run dev
```

### Frontend
In another terminal:
```bash
cd frontend
npm install
npm start
```

The frontend uses `http://localhost:4000/api` by default. The backend stores orders in memory for this demo and does not process real payments.
